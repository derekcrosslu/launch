let trees = ['birch', 'pine', 'sequoia', 'palm tree'];

const a = trees[trees.length - 1];
console.log("trees[trees.length - 1];: ", a);
const b = trees.pop()
console.log("trees.pop(): ", b);
const c = trees[trees.length - 1];
console.log("trees[trees.length - 1];: ", c);