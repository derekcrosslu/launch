// Is there a method to capitalize a string, for example turning 'mountain' into 'Mountain'?

const str = 'mountain';
const str1 = str[0].toUpperCase();
const out = str1.concat(str.slice(1));

const arr = ['fish', 'and', 'chips'];
console.log(arr[10]);

