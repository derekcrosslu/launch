let tweet = 'Woohoo! :-)';

// if (tweet.length() > 140) {
if (tweet.length > 140) {  // to fix code just remove parenthesis for property length
  console.log('Tweet is too long!');
}

//TypeError: tweet.length is not a function